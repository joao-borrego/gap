cmake_minimum_required(VERSION 3.0)

# Camera tools example client
add_subdirectory(camera_example)
# Visual tools example client
add_subdirectory(visual_example)
# World tools example client
add_subdirectory(world_example)
# Random scene generator
add_subdirectory(scene_example)
